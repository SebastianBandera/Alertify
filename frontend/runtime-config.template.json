{
  "apiBaseUrl": "${BACKEND_PUBLIC_URL}",
  "keycloak": {
    "url": "${KEYCLOAK_PUBLIC_URL}",
    "realm": "${OIDC_REALM}",
    "clientId": "${OIDC_FRONTEND_CLIENT_ID}",
    "rolesClientId": "${OIDC_BACKEND_AUDIENCE}"
  }
}

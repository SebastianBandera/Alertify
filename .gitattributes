# Keep text files consistent across operating systems and container builds.
* text=auto eol=lf

# Linux container entrypoints and helper scripts must retain Unix line endings.
*.sh text eol=lf
*.envsh text eol=lf

# Keep native Windows launchers using their expected line endings.
*.bat text eol=crlf
